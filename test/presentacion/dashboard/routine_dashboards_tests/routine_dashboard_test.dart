import 'package:flutter_test/flutter_test.dart';

// Tests de la página RoutineDashboardPage.
// La página está en construcción, los tests se irán agregando
// a medida que se implemente la UI.
void main() {
  group('RoutineDashboardPage', () {
    test('placeholder – la suite debe compilar correctamente', () {
      expect(true, isTrue);
    });
  });
}
