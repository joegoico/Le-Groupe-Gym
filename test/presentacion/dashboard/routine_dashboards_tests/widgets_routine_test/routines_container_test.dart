import 'package:flutter_test/flutter_test.dart';

// Tests del widget RoutinesContainer.
// El widget está en construcción, los tests se irán agregando
// a medida que se implemente la UI.
void main() {
  group('RoutinesContainer', () {
    test('placeholder – la suite debe compilar correctamente', () {
      expect(true, isTrue);
    });
  });
}
